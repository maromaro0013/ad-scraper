%h1 Target Sites

%table
  %thead
    %tr
      %th
        name
      %th
        domain
      %th
        create date

    - @target_sites.each do |target_site|
      %tr
        %td #{target_site.name}
        %td
          %a{ href: "https://#{target_site.domain}", target: "_blank" }
            #{target_site.domain}
        %td #{target_site.created_at.strftime("%Y-%m-%d %H:%M:%S")}
